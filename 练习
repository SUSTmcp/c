//判断1000-2000年的闰年
#include<stdio.h>
#include<stdlib.h>
int main()
{
	int year;
	for (year = 1000; year <= 2000; year += 2)
	{
		if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0)
		{
			printf("%d  ", year);
		}
	}
	printf("\n");
	system("pause");
	return 0;
}
